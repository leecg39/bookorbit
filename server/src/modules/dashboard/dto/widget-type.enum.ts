import { WIDGET_TYPE } from '@bookorbit/types';
import type { WidgetType as SharedWidgetType } from '@bookorbit/types';

export const WidgetType = WIDGET_TYPE;
export type WidgetType = SharedWidgetType;
